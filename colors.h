#ifndef COLORS_H
#define COLORS_H

#include <raylib.h>

#define COLOR_RGBA(R, G, B, A) ((Color) { .r = (R), .g = (G), .b = (B), .a = (A)})

#define CLEAR_COLOR COLOR_RGBA(0xCD, 0xCD, 0xCD, 0xFF)

#endif
